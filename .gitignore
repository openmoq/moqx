# Build outputs
/build/
/cmake-build-*/
/CMakeFiles/
CMakeCache.txt
/CMakeUserPresets.json
cmake_install.cmake
install_manifest.txt
compile_commands.json

# Ninja
.ninja_deps
.ninja_log

# CTest
Testing/
DartConfiguration.tcl

# CPack
_CPack_Packages/

# Local dependency staging: /.scratch/ holds the from-source superbuild's
# moxygen install; /.docker-deps/ holds docker dependency bundles.
/.scratch/
/.docker-deps/

# OS
.DS_Store

# IDE / tools
.vscode/
.claude/
.cache/
AGENTS.md

# Docker secrets (never commit)
docker/.env
docker/cloudflare.ini

# gh-pages dashboard (lives on gh-pages branch, not main)
/gh-pages/
docker/grafana/provisioning/dashboards/archive/
