# nginx denylist for the public dashboard port. One rule per line:
#   deny 203.0.113.7;
#   deny 198.51.100.0/24;
# Then reload:  docker exec moqx-proxy nginx -s reload
# fail2ban can manage this file automatically (see docker/grafana/README.md).
